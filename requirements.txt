# Pinned to the stack that produced the published frozen-test numbers.
# torch is left as a floor so Colab / MPS / CUDA wheels can differ.
transformers==5.15.0
datasets==5.0.1
trl==1.10.0
peft==0.20.0
accelerate==1.14.0
torch>=2.2.0
sentencepiece>=0.2.0
tqdm>=4.66.0
huggingface_hub>=0.26.0
